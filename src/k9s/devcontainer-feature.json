{
  "id": "k9s",
  "version": "0.1.0",
  "name": "k9s",
  "documentationURL": "https://k9scli.io/",
  "description": "Installs k9s, a terminal-based UI for Kubernetes clusters.",
  "options": {
    "version": {
      "type": "string",
      "proposals": [
        "latest",
        "0.51.0",
        "0.50.9",
        "0.50.7"
      ],
      "default": "latest",
      "description": "k9s version to install. 'latest' installs the newest release. Otherwise a published version such as '0.51.0' (a leading 'v' is stripped)."
    }
  }
}
