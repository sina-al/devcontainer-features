{
  "id": "devcontainers-cli",
  "version": "0.1.1",
  "name": "Dev Container CLI",
  "documentationURL": "https://github.com/devcontainers/cli",
  "description": "Installs the official Dev Container CLI (devcontainer), used to build, run, and test dev containers and author/tests dev container features and templates.",
  "options": {
    "version": {
      "type": "string",
      "proposals": [
        "latest",
        "0.88.0",
        "0.87.0",
        "0.86.1",
        "0.85.0"
      ],
      "default": "latest",
      "description": "Dev Container CLI version to install. 'latest' installs the newest release. Otherwise a published version such as '0.88.0' (a leading 'v' is stripped)."
    }
  }
}
